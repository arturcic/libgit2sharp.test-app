name: container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: gittools/build-images:ubuntu.20.04-sdk-6.0
    steps:
         - name: Log the node version
           run: |
             cat /etc/os-release 
             dotnet --info
